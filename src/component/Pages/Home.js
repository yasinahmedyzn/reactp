import React from "react";
import ControlledCarousel from "../Carousel/ControlledCarousel";

const Home = () => {
  return (
    <div>
      <ControlledCarousel />
    </div>
  );
};

export default Home;
